/**
 * OPTIONAL local override only.
 *
 * Primary source of truth is config/{env}_{mode}.json (includes platformSecret /
 * platformSign, same as legacy wechat/config). You normally do NOT need this file.
 *
 * If present, only non-empty fields overlay the tenant config — useful for a
 * one-off local appid/secret without editing the shared config JSON.
 *
 * Copy to tenants/local.secrets.json if you need it.
 */
{
  "xiaolongyu": {
    "platformSecret": "",
    "platformSign": "",
    "appId": ""
  },
  "demo": {
    "appPlus": {
      "amapKey": ""
    }
  }
}
